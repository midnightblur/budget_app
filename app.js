/**
 * The model
 */
var theModel = (function() {

    // Expose public API
    return {

    };
}());

/**
  * The view
  */
var theView = (function() {
    // Code update or read the UI
}());

/**
 * The controller
 */
var theController = (function(model, view) {
    // Controller code
}(theModel, theView));